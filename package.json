{
  "name": "leantable-platform",
  "version": "0.1.0",
  "private": true,
  "description": "LeanTable Platform - Monorepo for API, ML models, mobile app, web dashboard, and infrastructure",
  "workspaces": [
    "packages/api",
    "packages/shared",
    "packages/infrastructure",
    "packages/web"
  ],
  "scripts": {
    "build": "npm run build --workspaces",
    "test": "npm run test --workspaces",
    "lint": "npm run lint --workspaces"
  },
  "engines": {
    "node": ">=18.0.0"
  },
  "devDependencies": {
    "@aws-sdk/client-cognito-identity-provider": "^3.1003.0",
    "@aws-sdk/client-dynamodb": "^3.1004.0",
    "@aws-sdk/credential-providers": "^3.1004.0",
    "@aws-sdk/lib-dynamodb": "^3.1004.0"
  }
}
